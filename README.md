# RusticAgentic — Website

Public site for [rusticagentic.agency](https://rusticagentic.agency).

Deployed via GitHub Pages from `docs/` on `main` branch.

## Structure

```
docs/
  index.html   — single-page site (legal AI focus)
  CNAME        — custom domain: rusticagentic.agency
README.md
```

## Deploy

Push to `main`. GitHub Pages serves `docs/index.html` automatically.

Custom domain: `rusticagentic.agency`

DNS records (Namecheap) — see project notes.
